#!/usr/bin/env bash
set -euo pipefail

# Clear both active Wayland selections before wiping persisted history. The
# short delay lets the cliphist watchers observe the empty selection, then the
# final wipe removes anything they may have recorded during that transition.
wl-copy --clear
wl-copy --primary --clear
sleep 0.1
cliphist wipe
