#!/bin/bash

state_file="$HOME/.local/state/omarchy/current/font.name"

if [[ -s $state_file ]]; then
  cat "$state_file"
else
  sed -n '/<string>monospace<\/string>/,/<\/match>/{s:.*<string>\(.*\)</string>.*:\1:p}' \
    "$HOME/.config/fontconfig/fonts.conf" | tail -n 1
fi
