general {
    lock_cmd = $HOME/.local/bin/desktop-shell-action lock
    before_sleep_cmd = $HOME/.local/bin/desktop-shell-action lock
    inhibit_sleep = 3
}

listener {
    timeout = 180
    on-timeout = $HOME/.local/bin/desktop-shell-action lock
}
